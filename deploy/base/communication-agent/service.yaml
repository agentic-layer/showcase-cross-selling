apiVersion: v1
kind: Service
metadata:
  name: communications-agent
  namespace: use-case-cross-selling
  labels:
    app: communications-agent
spec:
  selector:
    app: communications-agent
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
