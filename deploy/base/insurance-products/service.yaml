apiVersion: v1
kind: Service
metadata:
  name: mcp-insurance-products
  namespace: use-case-cross-selling
  labels:
    app: insurance-products
spec:
  selector:
    app: insurance-products
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP