apiVersion: v1
kind: Service
metadata:
  name: mcp-customer-crm
  namespace: use-case-cross-selling
  labels:
    app: customer-crm
spec:
  selector:
    app: customer-crm
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP